[pytest]
# python_backend usa imports absolutos (from ocr..., from principal..., from motores.<motor>...)
# sem __init__.py (namespace packages), então este diretório (onde este .ini mora) precisa entrar no
# sys.path. testpaths restringe a coleta aos testes do projeto (evita varrer o dist/ congelado, com
# os testes do onnxruntime, caso exista localmente).
pythonpath = .
testpaths = tests
