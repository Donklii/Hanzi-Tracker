[pytest]
# python_backend usa imports absolutos (from ocr..., from principal...) sem __init__.py, então o dir
# precisa entrar no sys.path. testpaths restringe a coleta aos testes do projeto (evita varrer o
# python_backend/dist/ congelado, que traz os testes do onnxruntime).
pythonpath = python_backend
testpaths = python_backend/tests
